{
  "name": "@workflow/examples",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "haiku": "workflow run src/haiku.workflow.ts --yes",
    "vue-newsletter": "workflow run src/vue-newsletter.workflow.ts --yes"
  },
  "devDependencies": {
    "@workflow/cli": "workspace:*",
    "zod": "^4.0.0"
  }
}
